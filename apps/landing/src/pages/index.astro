---
import Layout from '@/layouts/Layout.astro';
import { Button } from '@workspace/ui/components/button';

// Project identity is data: the template is never renamed, so the name a user
// reads comes from the environment rather than from this file. The fallback
// matches project_display_name() in config/env.py, so a clone that set only the
// slug reads the same here as it does server-side.
const projectDisplayName =
  import.meta.env.PROJECT_DISPLAY_NAME ||
  (import.meta.env.PROJECT_SLUG || 'app')
    .replace(/[-_]+/g, ' ')
    .replace(/\b\w/g, (character: string) => character.toUpperCase());
---

<Layout title={projectDisplayName}>
  <main class="container mx-auto px-4 py-16">
    <!-- Hero Section -->
    <section class="flex flex-col items-center justify-center py-20 text-center">
      <h1 class="text-4xl font-bold tracking-tight sm:text-6xl">
        {projectDisplayName}
      </h1>
      <p class="mt-6 max-w-2xl text-lg text-muted-foreground">
        This is the way.
      </p>
      <div class="mt-10 flex gap-4">
        <Button asChild size="lg">
          <a href="/app/">Get Started</a>
        </Button>
        <!-- Both ship in the markup and CSS hides one, so the document stays
             identical for every visitor. src/session-hint.js decides which,
             from a cookie, before the first paint. -->
        <span data-when="signed-out">
          <Button variant="outline" size="lg" asChild>
            <a href="/accounts/login/">Sign In</a>
          </Button>
        </span>
        <span data-when="signed-in">
          <Button variant="outline" size="lg" asChild>
            <a href="/app/">Open App</a>
          </Button>
        </span>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-20">
      <h2 class="text-center text-3xl font-bold">Features</h2>
      <div class="mt-12 grid gap-8 md:grid-cols-3">
        <div class="rounded-lg border p-6">
          <h3 class="text-xl font-semibold">Feature One</h3>
          <p class="mt-2 text-muted-foreground">
            Description of the first feature and why it's valuable.
          </p>
        </div>
        <div class="rounded-lg border p-6">
          <h3 class="text-xl font-semibold">Feature Two</h3>
          <p class="mt-2 text-muted-foreground">
            Description of the second feature and why it's valuable.
          </p>
        </div>
        <div class="rounded-lg border p-6">
          <h3 class="text-xl font-semibold">Feature Three</h3>
          <p class="mt-2 text-muted-foreground">
            Description of the third feature and why it's valuable.
          </p>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 text-center">
      <h2 class="text-3xl font-bold">Ready to get started?</h2>
      <p class="mt-4 text-muted-foreground">
        Create your account today and start building.
      </p>
      <Button asChild size="lg" className="mt-8">
        <a href="/accounts/signup/">Sign Up Free</a>
      </Button>
    </section>
  </main>

  <!-- Footer -->
  <footer class="border-t py-8">
    <div class="container mx-auto px-4 text-center text-sm text-muted-foreground">
      <p>
        &copy; {new Date().getFullYear()} {projectDisplayName}. All rights reserved.
      </p>
    </div>
  </footer>
</Layout>
